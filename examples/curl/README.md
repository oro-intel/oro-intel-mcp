# curl

Resolves a UK company by name via the Oro Intel REST API, then fetches its full supplier profile, printing `credits_charged`/`credits_remaining`.
Run: `ORO_API_KEY=oro_... ./company-profile.sh "Serco"` (uses the REST API directly).
